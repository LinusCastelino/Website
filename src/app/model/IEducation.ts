export interface IEducation {
    eventHeader: {
        batch: string;
        details: string[];
    };

    eventInfo: {
        univ: string;
        details: string[];
    };

    eventLogoUrl: string;
}
